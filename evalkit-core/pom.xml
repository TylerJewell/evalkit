<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.akka.evalkit</groupId>
    <artifactId>evalkit-core</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>evalkit-core</name>
    <description>Scoring, outcomes and reporting. No runtime dependency of any kind.</description>

    <url>https://github.com/akka/evalkit</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Akka</name>
            <organization>Lightbend, Inc. dba Akka</organization>
            <organizationUrl>https://akka.io</organizationUrl>
        </developer>
    </developers>

    <scm>
        <url>https://github.com/akka/evalkit</url>
        <connection>scm:git:https://github.com/akka/evalkit.git</connection>
        <developerConnection>scm:git:git@github.com:akka/evalkit.git</developerConnection>
    </scm>

    <issueManagement>
        <system>GitHub Issues</system>
        <url>https://github.com/akka/evalkit/issues</url>
    </issueManagement>

    <!--
      No dependencies, and that is the point rather than an accident. What a run
      may claim, how an outcome is classified and what the report refuses to say
      are decisions about evidence, not about infrastructure. Adding a dependency
      here should require an argument.
    -->
    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.1</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>3.24.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.22.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
        </plugins>
    </build>
</project>
