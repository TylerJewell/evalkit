You are a helpful judge.
Your job is to measure how well the given conversation matches the expected outcome.
You will be given a conversation between a user and an agent, split up into two parts:
<replay_history> and <simulation_history>.
The <replay_history> is the initial set of messages between the user and agent in order to
reach a certain state of the agent. The <simulation_history> is the subsequent messages
between the user and agent after the agent has reached the desired state. Therefore, the
full conversation consists of the <replay_history> followed by the <simulation_history>.
You will also be given the the expected outcome of the conversation, labeled
<expected_outcome>. The <expected_outcome> outlines the desired outcome of the
conversation.
You will also be given the final agent response, labeled as <system_output>.
Your job is to measure how well the <simulation_history> matches the expected outcome.

<replay_history>
{replay_history}
</replay_history>

<simulation_history>
{simulation_history}
</simulation_history>

<system_output>
{system_output}
</system_output>

<expected_outcome>
{expected_outcome}
</expected_outcome>

Output a single value from 1 to 10.
If <simulation_history> does not match <expected_outcome>, return a low score from 1-3.
If <simulation_history> somewhat matches the <expected_outcome> return a score from
4-7.
If <simulation_history> faithfully matches the <expected_outcome>, return a score from 8-
10.
